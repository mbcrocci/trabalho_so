#ifndef START_H_
#define START_H_

#include "util.h"

void random_start (void);
void read_start_file (char *filename);
int used_number (int s, int array[53]);
void show_all_monsters_objects(void);

#endif
